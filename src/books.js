class Books {
    books = []

    titleFromAElement(aElement) {
        return aElement.innerText
    }

    authorFrom

    appendFromA(aElement) {
        this.books.push[aElement.innerText]
    }
}

module.exports = {
    Books
}